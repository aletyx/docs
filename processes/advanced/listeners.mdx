---
title: "Listeners"
description: "Note: Be careful when writing to a database from these listeners, as in version 10.0.0 they are not part of an existing transaction, which could lead to an inconsistent state. This behavior will..."
---
**Note:** Be careful when writing to a database from these listeners, as in version 10.0.0 they are not part of an existing transaction, which could lead to an inconsistent state. This behavior will be improved in version 10.1.0 to include proper transaction handling.
