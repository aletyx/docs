---
title: "API reference"
description: "Integrate Aletyx with your systems."
---

The Aletyx API lets you invoke deployed decisions, start and manage process
instances, and query execution history programmatically.

<Note>
  Add an `openapi` entry to `docs.json` to auto-generate endpoint pages from
  your OpenAPI specification.
</Note>
